<%= form_for(@neighbor) do |f| %>
  <% if @neighbor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@neighbor.errors.count, "error") %> prohibited this neighbor from being saved:</h2>

      <ul>
      <% @neighbor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
